import { FiltrarSHPipe } from './filtrar-sh.pipe';

describe('FiltrarSHPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarSHPipe();
    expect(pipe).toBeTruthy();
  });
});
